name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: dtolnay/rust-toolchain@master
      with:
        toolchain: nightly-2023-03-24
        components: rust-src
    - run: sudo apt-get update && sudo apt-get install binutils-arm-none-eabi
    - run: curl -LsSf https://github.com/taiki-e/cargo-hack/releases/latest/download/cargo-hack-x86_64-unknown-linux-gnu.tar.gz | tar xzf - -C ~/.cargo/bin
    - run: curl -LsSf https://github.com/mgba-emu/mgba/releases/download/0.10.2/mGBA-0.10.2-ubuntu64-jammy.tar.xz | tar -C . -xJv
    - run: cp mGBA-0.10.2-ubuntu64-jammy/mgba-qt.deb mgba.deb
    # - run: cargo hack test --feature-powerset
    - run: cargo test --all-features --workspace
